# Note: torch, torchvision, numpy, pandas, matplotlib, seaborn, scikit-learn,
# scikit-image, Pillow, tqdm, h5py, pyyaml, jupyter are inherited from the
# Anaconda base via --system-site-packages, so they are not re-installed here.

# Additions for the project
opencv-python==4.10.0.84
albumentations==1.4.18
timm==1.0.11

# Deployment
gradio==4.44.1
huggingface_hub>=0.25,<1.0
fastapi==0.115.4
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# Export
onnx==1.17.0
onnxruntime==1.20.1
coremltools==8.1
